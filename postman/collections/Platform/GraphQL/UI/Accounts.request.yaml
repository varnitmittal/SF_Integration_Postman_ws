$kind: graphql-request
url: "{{_endpoint}}/services/data/v{{version}}/graphql"
query: |-
  query accounts {
    uiapi {
      query {
        Account {
          edges {
            node {
              Id
              Name {
                value
              }
            }
          }
        }
      }
    }
  }
headers:
  X-Chatter-Entity-Encoding: "false"
examples: ./.resources/Accounts.resources/examples
order: 1000
