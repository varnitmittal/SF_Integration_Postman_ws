$kind: graphql-request
url: "{{_endpoint}}/services/data/v{{version}}/graphql"
query: |-
  mutation AccountExample{
    uiapi {
      AccountCreate(input: {
        Account: {
          Name: "Trailblazer Express"
        }
      }) {
        Record {
          Id
          Name {
            value
          }
        }
      }
    }
  }
headers:
  X-Chatter-Entity-Encoding: "false"
examples: ./.resources/Create Account.resources/examples
order: 1000
