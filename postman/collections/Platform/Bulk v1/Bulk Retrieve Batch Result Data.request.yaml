$kind: http-request
url: "{{_endpoint}}/services/async/{{version}}/job/{{_jobId}}/batch/{{_batchId}}/result/:batchResultId"
method: GET
headers:
  X-SFDC-Session: "{{_accessToken}}"
  Accept-Encoding: gzip
  Content-Encoding: gzip
pathVariables:
  batchResultId: ""
examples: ./.resources/Bulk Retrieve Batch Result Data.resources/examples
order: 8000
