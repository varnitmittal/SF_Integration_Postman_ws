$kind: http-request
url: "{{_endpoint}}/services/data/v{{version}}/tooling/query/?q=SELECT FIELDS(ALL) FROM EventRelayConfig LIMIT 200"
method: GET
queryParams:
  q: SELECT FIELDS(ALL) FROM EventRelayConfig LIMIT 200
body:
  type: json
  content: ""
settings:
  strictSSL: false
order: 1000
